<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.app.id.presiden">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".jokowi">
        </activity>
        <activity android:name=".sby">
        </activity>
        <activity android:name=".megawati">
        </activity>
        <activity android:name=".gusdur">
        </activity>
        <activity android:name=".habibie">
        </activity>
        <activity android:name=".soeharto">
        </activity>
        <activity android:name=".soekarno">
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>